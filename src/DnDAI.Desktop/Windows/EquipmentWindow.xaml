<Window x:Class="DnDAI.Desktop.Windows.EquipmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Equipment &amp; Inventory" Height="700" Width="900"
        Background="#ECF0F1" WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Window.Resources>
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>

        <Style x:Key="EquipmentButton" TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2C3E50" CornerRadius="8" Padding="20" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock x:Name="CharacterNameText" Text="Character Name"
                               FontSize="20" FontWeight="Bold" Foreground="White"/>
                    <TextBlock x:Name="WeightText" Text="Weight: 0 / 0 lbs"
                               FontSize="14" Foreground="#BDC3C7" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Gold:" FontSize="14" Foreground="#BDC3C7" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock x:Name="GoldText" Text="0" FontSize="20" FontWeight="Bold"
                               Foreground="#F39C12" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Equipped Items -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="Equipped" Style="{StaticResource SectionHeader}"/>
                <ScrollViewer MaxHeight="150" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="EquippedPanel">
                        <TextBlock Text="No items equipped" FontStyle="Italic" Foreground="#7F8C8D" Margin="5"/>
                    </StackPanel>
                </ScrollViewer>
            </StackPanel>
        </Border>

        <!-- Inventory -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <TextBlock Text="Inventory" Style="{StaticResource SectionHeader}" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="+ Add Standard" Width="140" Style="{StaticResource EquipmentButton}"
                                Click="AddStandardEquipment_Click"/>
                        <Button Content="+ Add Custom" Width="130" Style="{StaticResource EquipmentButton}"
                                Background="#9B59B6" Click="AddCustomEquipment_Click"/>
                    </StackPanel>
                </Grid>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="InventoryPanel" HorizontalAlignment="Stretch">
                        <TextBlock Text="No items in inventory" FontStyle="Italic" Foreground="#7F8C8D" Margin="5"/>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Footer Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Close" Width="100" Height="35" Background="#95A5A6" Foreground="White"
                    BorderThickness="0" Click="Close_Click" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
